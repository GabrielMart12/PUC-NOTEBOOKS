1.)
A = π actors.first_name, actors.last_name (actors)
B = π directors.first_name, directors.last_name (directors)
C = A ∩ B
π actors.first_name, actors.last_name (C)

2.)
A = π actors.first_name, actors.last_name (actors)
B = π directors.first_name, directors.last_name (directors)
C = A - B
π actors.first_name, actors.last_name (C)

3.)
A = π actors.first_name, actors.last_name (actors)
B = π directors.first_name, directors.last_name (directors)
C = A ∪ B
π actors.first_name, actors.last_name (C)

4.)
A = π directors.id (directors)
B = π movies_directors.director_id (movies_directors)
C = ρ idd ← id (A - B)
D = C ⨝ idd = movies_directors.director_id (movies_directors)
E = D ⨝ movies_directors.movie_id = movies.id (movies)
π movies.name (E)

5.)
A = γ roles.actor_id; COUNT(roles.movie_id)→tot_movies (roles)
B = π roles.actor_id (σ tot_movies >= 2 (A))
C = π actors.id (actors)
D = ρ ida ← id (C - B)
E = D ⨝ ida = actors.id (actors)
π actors.first_name, actors.last_name (E)

6.)
A = γ roles.actor_id; COUNT(roles.movie_id)→tot_movies (roles)
B = σ tot_movies >= 2 (A)
C = π roles.actor_id (B)
D = π actors.id (actors)
E = ρ ida ← actors.id (D - C)
F = E ⨝ ida = roles.actor_id (roles)
G = F ⨝ roles.movie_id = movies.id (movies)
H = G ⨝ movies.id = movies_genres.movie_id (movies_genres)
I = γ movies_genres.genre, movies.year; COUNT(movies.id)→tot_final (H)
π movies_genres.genre, movies.year, tot_final (I)

A = γ roles.movie_id; COUNT(roles.actor_id)→tot_actors (roles)
B = σ tot_actors >= 2 (A)
C = π roles.movie_id (B)
D = π movies.id (movies)
E = ρ idm ← movies.id (D - C)
F = E ⨝ idm = movies.id (movies)
G = F ⨝ movies.id = movies_genres.movie_id (movies_genres)
H = γ movies_genres.genre, movies.year; COUNT(movies.id)→tot_final (G)
π movies_genres.genre, movies.year, tot_final (H)
